"In our UI, the player should be able to play the game by clicking on buttons rather than typing their answer in a prompt."

Add a title, explanation of game concept with image and "start the game" instructions. 

Add a div "button bar" to contain all the 6 buttons. 

Add 3 buttons, 1 for each player selection ("rock", "paper" or "scissors"). Give each one an icon and style.

Add 3 more buttons for the computer's selection, this is the selection of "the cat called Binky" so these are the "Binky Buttons". Add icons and style. 

Assign a specific button to each player selection. 

Add an event listener to the player buttons that call your playRound function with the correct playerSelection every time a button is clicked. 

Assign a specific "Binky button" to each random word generated by computerPlay() ("rock", "paper" or "scissors"). 

Add a glow effect to indicate each specific "Binky button chosen" by highlighting it in the UI when computerPlay() returns its assigned word. Turn glow off again when computerPlay() fires next.  

Add a div that displays the restart button. Hide it until it appears at the end of the game to replace the 6 selection buttons. 

Add a div for displaying results and change all of your console.logs into DOM methods.

Add a div to display the running score, and announce a winner of the game once one player reaches 5 points (change all console logs into DOM methods). 